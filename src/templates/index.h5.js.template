/* eslint-disable */

import React from 'react';
#rpx-1:import Taro from '@tarojs/taro';:#
import Icon from './h5';

const IconFont = (props) => {
  const { name, size, color, style ,className,onClick} = props;

  return <Icon name={name} size={#rpx-1:parseFloat(Taro.pxTransform(size, #designWidth#)):##rpx-0:size:#} color={color} style={style} className={className} onClick={()=>onClick&&onClick()}/>;
};

IconFont.defaultProps = {
  size: #size#,
};

export default IconFont;
